// The engine (this admin app: views/js/css) version. Bumped on every engine
// change so the baker can tell which build each project runs after she syncs
// the engine across her sites (bakeadmin, munchies, ...). Shown on More.
export const ENGINE_VERSION = "71";
