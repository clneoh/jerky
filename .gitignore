# Private / tooling files — never publish these.
.claude/
.DS_Store
node_modules/

# Marketing PDF — keep it off the public site.
marketing/

# Owner's original source photos (large files) — never publish. Keep images/ (the
# optimised web copies) tracked instead.
homepage-photos/
