# Jerky (dehydrated pet treats) — project start

This is a NEW, separate project for the owner's second home-based business: dehydrated pet treats ("jerky"). It is a fresh start on purpose — its own folder, its own git history, its own Claude memory. Do not assume anything here is the bakery.

## The one big fact to know first

This project is a DUPLICATION of the existing bakery system, which lives at:

`/Users/clneoh/Downloads/bakeadmin`

That repo contains: a static shop/storefront, a single-page admin app (`/admin/`), a marketing-guide generator (`marketing/build_guide.py` → `bakester-marketing-guide.pdf`), and Supabase sync. The bakery's live domain is jienluv2bake.com.my (GitHub Pages: root = homepage, /store/ = shop, /admin/ = app).

**The duplication is DONE.** The whole setup was copied into this folder and adapted to dehydrated pet treats (same mechanics — GitHub Pages + Supabase — but its OWN repo, its OWN Supabase project, its OWN domain name, never the bakery's). See "Status" below for where the project stands.

## The owner

- A Malaysian home-based producer, NON-technical. Wants plain-English, step-by-step guidance, and to test on her phone (and Mac).
- Deploys herself via GitHub Desktop — see the rules below.
- She also runs the bakery (jienluv2bake.com.my); this is her second business.

## Standing rules (carried from the bakery — apply to all work here)

- **Never `git commit` or `git push`.** The owner deploys via GitHub Desktop. Report changes and suggest a commit summary instead.
- **Never ship or embed the Supabase app-login email/password.** The owner types both in on each phone (confirmed wanted).
- Keep private keys out of the repo (e.g. any ntfy topic).
- The marketing guide is the SOP — keep it in sync with every change (`build_guide.py`, rebuild, re-send the PDF). `marketing/` is gitignored. **Its version FOLLOWS THE ENGINE** (read live from `admin/js/version.js`): an engine build is `v<N>` (e.g. v123); a rebuild that is ONLY a manual/content change adds a letter — `v123a`, `v123b`… — reset to plain `v<N+1>` on the next engine sync. `build_guide.py` reads the engine number itself; just set the PDF-only `LETTER` (currently `"c"` — the engine is v123 and the 2026-09-20 sales-codes/QR-labels work was built here alone, in three same-day revisions, so the manual is **plain v123c**; the engine number deliberately did NOT move, and a bakery→jerky sync is always picked by blob hash, never by number). `marketing/build_changelog.py` reads the same number, so the change-history PDF's banner and footer can never go stale.
- **ALWAYS refresh every marketing PDF on any update** (owner's standing rule, 2026-09-17). Run **`python3 marketing/build_all.py`** — one command, rebuilds all five docs (the two manuals, the root `changelog.pdf`, the postage explainer, the market survey) and reports page counts and failures; `--check` lists what exists and how old. Never hand-pick builders — a doc left behind is worse than no doc. `market_survey.py` is the exception: its prices are **dated research**, so do not refresh its numbers unprompted — ask her first. Also sweep the **repo root**, not just `marketing/`: the only PDF that belongs there is the tracked, published `changelog.pdf`. (A stale `furkidz-marketing-guide.pdf` once sat at the root and was served publicly; she had it deleted on 2026-09-17. The builders' `OUT` paths are now anchored to `marketing/`, so a copy cannot reappear there — keep them anchored.)
- Verify only against an isolated throwaway origin (sandbox), never real data.
- Only create commits when the user explicitly asks.
- The bakery's **real** Supabase url/anonKey and its CNAME domain must NOT be copied into this repo. jerky keeps its OWN Supabase values (project `ircwozniiyywsowamixy`, in `store/config.js` + `admin/js/state.js` `BUILTIN_SUPABASE`). Her domain is final — **`munchies.com.my`** (apex) — set in `CNAME`.

## Owner decisions (confirmed 2026-09-06)

1. **Business name: "Munchies Furkidz"** — display brand, kept. **Domain: `munchies.com.my`** (apex, no "www") — registered/active 2026-09-07, set in `CNAME`. She may still fine-tune the exact display wording later; if so, rebuild the guide PDF and update branding single-points.
2. **Delivery: post nationwide** (shelf-stable jerky). **Flat postage fee per order**, default RM8 — added by the owner at confirmation, NOT auto-added to the storefront total. Stored as `settings.storefront.postageRM` (default 8), edited in admin **Settings → Storefront → Postage (nationwide posting)**, read by the WhatsApp confirm/payment builders. It syncs between HER phones via the PRIVATE shared-data settings row (last person who set it wins, so both quote the same) but is deliberately **never published** to the public storefront. Emitting it from a phone is gated by `settings.storefront.postageSet === true` — a phone still at the default 8 must not overwrite the value she set on another phone (same guard as the tasks list).
3. **Delivery-date mechanics are IDENTICAL to bakeadmin**: `deliveryDays [1,3,5]` (Mon/Wed/Fri), cutoff 18:00, same date picker/capacity/limits. Copy-only relabel: the picked day is her batch/posting day.
4. **Back-office: copied entirely** (orders, products + cost recipes, ingredients, PO, suppliers, customers, referrals).
5. **Product form: pouches by weight** (e.g. "100g pouch").

## Duplication vocabulary (internal ids are KEPT — no schema migration)

| Concept | Internal value | User-visible wording |
|---|---|---|
| Brand | `Bakester`-era keys kept origin-scoped | Munchies Furkidz, paw 🐾 |
| Status | `baking` | **Preparing** (journey: New → Confirmed → Paid → Preparing → Packed → Delivered) |
| Fulfilment | `courier` / `collect` | **Post (nationwide)** / **Collect (local)** |
| Units | generic free-text `unit` | pouch, pack, jar, box, bag, set, piece (+ pcs) |
| Postage | `settings.storefront.postageRM` (+ `postageSet` gate) | flat per-order fee, on the WhatsApp To-pay line; private phone-to-phone sync (last-set wins), never on the storefront |
| Backup marker | `o.app` | `"furkidz"`, file `furkidz-backup-*.json` |
| Customer-profile fields | `dogName` / `dogPhoto` (kept, like status ids) | **pet's name** / pet photo; 🐾; customers CSV `munchies-furkidz-customers-*.csv` |
| Change history | — | `CHANGELOG.md` (localized v54→v123) → root `changelog.pdf` (via gitignored `marketing/build_changelog.py`, which reads the engine number live), linked from More → Full change history |

Internal storage keys (`localStorage` `bakeadmin.v1` / `bakeadmin.sync` / `bakeadmin.supabase`, sw.js cache `bakeadmin-admin-v1`) are **deliberately unchanged** — they are origin-scoped, so no collision with the bakery on separate domains/ports; renaming adds risk for no user-visible gain.

## Status

**Done so far:**
- Copied code tree, neutralised bakery identity (CNAME empty, Supabase creds → `"FILLME"`).
- Adapted homepage, storefront, and admin visible copy + units + sample data to pet treats / nationwide post (README and this file document it).
- **Domain final: `munchies.com.my`** (2026-09-07) — brand kept "Munchies Furkidz"; `CNAME` now contains `munchies.com.my`; marketing guide rebuilt as v2 with the domain; referral/track links auto-build from `location.origin` so no code changes needed.
- Full test suite green: `node --test test/*.test.js` → **1044 tests, all passing** (1029 pre-landing-pages; 1020 pre-codes-draft-fix; 910 pre-sales-codes; 872 pre-v119-123-sync; 865 pre-v116-118-sync; 862 pre-v114-115-sync; 805 pre-v103-113-sync; 789 pre-v99-102-sync; 778 pre-v97-98-sync; 775 pre-null-fix; 769 pre-v94-96-sync; 765 pre-v93-sync; 747 pre-v89-92-sync; 647 pre-v79-88-sync; 607 pre-v73-78-sync; 598 pre-v71-sync; 578 pre-v68/69-sync; 568 pre-v67-sync; 528 pre-v66-sync; 501 pre-v65-sync; 470 pre-v63-sync; 436 pre-v61-sync).
- **Engine synced to v61** (2026-09-08) — bakery HEAD → jerky, wholesale for the business-agnostic engine files: occasion import catalogue + whole-group ticking (`occasion_catalog.js`, `deliveries.js`, `.mycal-*` CSS), homepage customer reviews with approve-first moderation (`views/reviews.js`, root `reviews.js`, homepage section, `supabase/reviews.sql`), and cloud backups Daily/Weekly/Monthly/manual with restore/download/delete (`backups.js`, `Backup & safety` card, `supabase/backups.sql`). jerky-localized files hand-merged (app.js, supabase.js, settings.js, more.js, guide.js, README). Marketing guide rebuilt as plain v61.
- **Engine synced to v63** (2026-09-09) — bakery HEAD 3f39105→62cf7e0 → jerky, **code-only (no SQL)**: v62 amber "Not sharing right now" strip on every screen (`sharewarn.js`) + read-only **View** of each backup copy (`snapshot.js`, Settings row buttons), and v63 **5-tab swap** (Customers becomes a bottom tab, PO moves under More) with customer **profiles** (pet name/photo/likes/avoid/note + finder; `profiles.js`, `photo.js` shrink, views/customers.js pet wording), always-on Home **Upcoming holidays** card (`views/dashboard.js`, `.hol-list--scroll`), and More **Engine pill + Software wish list + Full change history** linking a new root `changelog.pdf` (`wishlist.js`, `views/more.js`, localized `CHANGELOG.md` + `marketing/build_changelog.py`, gitignored). Hand-merged keeping localization in state.js, sync.js (customers LISTS + wishList guard; postage hunks intact), backups.js, dashboard/settings/more/guide/app.js, homepage index.html (review-photo shrink), app.css. Marketing guide + Guide screen updated to the v63 UI; marketing guide rebuilt as plain v63 (21 pages).
- **Engine synced to v65** (2026-09-09) — bakery HEAD 62cf7e0→8a09b11 → jerky, **code-only (no SQL)**. **v64:** trilingual **EN / 中文 / BM** switch on homepage + order page (`i18n.js`, `home-lang.js`, `store-lang.js`, `home.js`, `store/app.js`), optional per-product **Shop names** (`nameZh`/`nameMs`), homepage reviews as an automatic swipeable **carousel** (`reviews.js`), and a **Website & developer** credit — Settings card sets name/emails/WhatsApp; "Website by …" shows on homepage + store footer and More → About gains WhatsApp/email rows; the software wish list emails the developer (`devmail.js`, optional `supabase/functions/wish-mail` Edge Function + manual mailto row). **v65:** More → Reviews shows each review **one at a time** exactly as it will render (waiting-to-publish first; Publish/Take down/Delete moves on), with "N waiting" pills on More and a Home review card (`views/reviews.js` byte-identical, `views/dashboard.js` +45 s refresh). Hand-merged keeping localization (postage hunks, Munchies wording in state/sync/more/dashboard/settings/app.js, homepage/store index.html, app.css). Guide screen "Newest in the engine" cards, CHANGELOG + root `changelog.pdf` rebuilt, README; marketing guide rebuilt as plain v65 (22 pages).
- **Engine synced to v66** (2026-09-09) — bakery HEAD e4ac869 → jerky, **code-only (no SQL)**. **v66:** product shop-text (name, description, selling unit, feeding tip) **auto-translates** into 中文 + Bahasa Malaysia via the free MyMemory API when saved/published online (`translate.js` new, `productState.js` new, `i18n.js` gains `descFor`/`servingFor`/`unitFor`; provenance `trOverride`/`trSrc`, "auto" tags, type-over-a-box = hers forever, "Fill all 中文 / BM" + ↻ buttons in Products → Edit), products get **three states** as three lists — **Draft / On the shop / Hidden** (a new product starts as a Draft, `draft:true`+`active:false`; never on storefront/availability/order pickers), and the **"Copy follow-up"** referral message gains an **EN / 中文 / BM** picker (`followup-lang.js` new, jerky-localized English line reads "Feeding tip"). Hand-merged keeping localization (postage hunks, Munchies wording in supabase/backups/referrals/customers/orders/products/snapshot.js + store/app.js; jerky post-default + postal-address store flow intact; followup-lang en.serving → "Feeding tip"). Guide screen three v66 "Newest in the engine" cards, CHANGELOG v54→v66 + root `changelog.pdf` rebuilt (3 pp), README; marketing guide rebuilt as plain v66 (22 pages).
- **Engine synced to v67** (2026-09-10) — bakery HEAD e4ac869→3a5755b → jerky, **code-only (no SQL)**. **v67:** a **"No name" customer keeps the name you save on their profile** — the customer book is built from orders, so an order with no name showed "(no name)" even after the profile was named; now `profiles.js` `customerRowName(row)` returns the derived name when the order carried one, else the saved profile name (falls back to "(no name)"), and the **row, the finder list, the history pop-up title and the "Hi {name}!" WhatsApp greeting** all use it; saving a profile refreshes the list at once (`onSaved` → `drawList`). Wholesale/new: `profiles.js` (+`customerRowName`), `version.js`(→67), `test/profiles.test.js`. Hand-merged keeping localization: `views/customers.js` (same 5 hunks — import, `openChat`, row title, `profileBlockEl` `onSaved`, `openHistory`; pet wording/🐾/Post-Collect CSV intact), `test/backups.test.js` (engine pin 66→67). The bakery's CORS fix `63cbc57` and `supabase/.temp/*` were **NOT ported** (jerky already has its own CORS-fixed `supabase/functions/wish-mail/index.ts` from owner commit 41e054b; `.temp` is private). Guide screen one v67 "Newest in the engine" card; CHANGELOG v54→v67 (prepended v67 + the 9 Sep wish-list-auto-email note); root `changelog.pdf` rebuilt (4 pp); marketing manual rebuilt plain v67 (22 pp). **568 tests pass** (bakery 562).
- **Engine synced to v68+v69** (2026-09-10) — bakery HEAD 3a5755b→b4c3c59 → jerky, **code-only (no SQL)** (ported in one go: v68 then its same-hour revision v69). **v68 — one name + one number per customer:** a customer's name/number were stored twice (orders + card) and could disagree; now the card writes through to every order (`profiles.js` `applyContact`/`syncContactFromOrder`/`reconcileContacts`; a blank box never wipes a needed number; a changed number re-points the person's referral credits; `upsertProfile(state, draft, fromKey)` re-keys as it writes), the **Order screen's Edit** carries a fix back to the card (`views/orders.js`), and a **one-time catch-up** fixes people renamed before v68 (`app.js` `migratedV68` → `reconcileContacts`). **v69 — either place updates the customer:** the last edit wins, not the card (`customerRowName` now compares `prof.orderEditAt` vs `prof.updatedAt`); the customer list re-derives on every draw so a rename refreshes at once (`views/customers.js` `visibleRows(now)`). Wholesale/new: `profiles.js`, `version.js`(→69), `test/profiles.test.js`. Hand-merged keeping localization: `app.js` (import + the `migratedV68` block; `/guide` route intact), `views/orders.js` (2 imports + the `syncContactFromOrder` call; Preparing/Post-Collect/pouch wording intact), `views/customers.js` (re-derive-on-draw + `fromKey` arg + a hint line; pet wording/🐾 intact), `test/backups.test.js` (now pins `ENGINE_VERSION` instead of a literal — no longer needs a manual bump each sync). Guide screen two v68/v69 "Newest in the engine" cards; CHANGELOG v54→v69 (prepended v69+v68); root `changelog.pdf` rebuilt (4 pp); README profiles paragraph updated; marketing manual rebuilt plain v69 (22 pp). **578 tests pass** (bakery 571).
- Sandbox-verified on an isolated localhost port: homepage, storefront order flow, admin offline sign-in, Products units, Settings postage card, Settings Backup & safety cloud-copies card, EN↔中文↔BM switch on homepage + order page, empty-reviews state, hidden dev credit, mobile 375 px no overflow. No broken local assets; no console errors.
- **Engine synced to v70** (2026-09-10) — bakery HEAD b4c3c59→2588b99 → jerky, **code-only (no SQL)**. **v70 — an order keeps the name and price it was sold at:** an order row now carries a **frozen `productName` + `unitPrice`** written the moment it is taken (from the shop or typed in by hand), so renaming or re-pricing a product changes the shop, never history — last month's order, the printed label, the customer's own track page and their lifetime spend all keep the sold name/price, and a product deleted later still shows what it was that someone bought. New pure helpers in `state.js` (`orderLineName` — frozen name, else live product, else "(deleted product)"; `orderLinePrice` — frozen price, else live, else **null** so "free" ≠ "unknown"; `stampOrderLine(o, product)` — never invents). Every reader rewritten to prefer the snapshot: `confirm.js`, `messages.js`, `supabase.js` (`trackingSnapshot` + `importIncoming` keeps the shop's own `line.price`), `customers.js` (`spend`), `weekly.js`, `views/orders.js` (row/label/inbox/finder/remove label; **only a line swapped to a DIFFERENT product re-prices** — an untouched line keeps its old price), `views/customers.js` (`productName(state, o)` now takes the order, not the id). One-time catch-up `app.js` `migratedV70` stamps pre-existing orders with today's values (a gone product / no price is left alone). Wholesale (blob == base): `admin/js/customers.js`, `admin/js/version.js` (→70), `admin/js/weekly.js`, `test/order-line.test.js` (new — the one bakery-domain fixture localized to `bake.app`). Hand-merged keeping localization: `app.js` (import + `migratedV70` block; `/guide` route intact), `confirm.js`/`messages.js` (items + the jerky `subtotal`→postage structure intact; Post/Collect wording untouched), `supabase.js` (tracking items/total + import `stampOrderLine`; "Post (nationwide)"/"Collect (local)" intact), `views/orders.js` (11 hunks; Preparing/Post-Collect/pouch wording + the v68 `syncContactFromOrder` call intact), `views/customers.js` (import + `productName(state, o)` + history line; pet wording/🐾 intact), `test/app.test.js` + `test/multiadd.test.js` + `test/supabase.test.js` (appended v70 cases; Munchies/localized fixtures kept). Guide screen one v70 "Newest in the engine" card; CHANGELOG v54→v70 (prepended v70); root `changelog.pdf` rebuilt (5 pp); README new "An order is a record of a sale" section; marketing manual plain v70 (22 pp) with the new order-record paragraph. **598 tests pass** (bakery 592).
- **Engine synced to v71** (2026-09-10) — bakery HEAD 2588b99→0443f96 → jerky, **code-only (no SQL)**. **v71 — the shop's EN / 中文 / BM buttons answer instantly:** tapping a language on the order page used to call `window.location.reload()`, so each tap re-downloaded the page and re-fetched the menu, the "left today" numbers and the storefront settings (a pause on a phone, and a reload could wipe a half-filled basket). Now `store/app.js` switches **in place**: `render()` assigns a `repaintForLang` hook (`applyTo(document, STORE, l)` + `renderStatic(CONFIG)` + `rerender()` + `renderBar()` + `paintTrack()`), the track card keeps its last result in `lastTrack` (`{kind, code, row}`) so `paintTrack()` redraws from it with **no network**, and an exported `setLang(next)` validates via `isLang(next)`, remembers the choice, runs the repaint and moves the pill highlight via `paintPillsHook` — returning true only when the language actually changed. `i18n.js` gains a `document.documentElement` guard in `applyTo`. Wholesale (blob == base): `admin/js/version.js` (→71), `i18n.js`, `test/store-lang-switch.test.js` (new — localized: the sample price RM15→**RM22** and "Focaccia"→"Chicken Jerky"). Hand-merged keeping localization: `store/app.js` (patch-applied; all 8 jerky deltas verified intact — "Posting days" comment, 🐾, "Post (nationwide)"/"Collect (local)", the postal-address-required `showConfirm` block, `fulfillment` default `|| "courier"`, reset-to-courier, `addrField.hidden = false`, "Preparing" comments, `apply("courier")`). Guide screen one v71 "Newest in the engine" card; CHANGELOG v54→v71 (prepended v71); root `changelog.pdf` rebuilt (5 pp); README v71 paragraph; marketing manual plain v71 (22 pp) with the "instant switch" sentence in section 4. **607 tests pass** (bakery 601).
- **Follow-up, no engine bump** (2026-09-10) — bakery HEAD 0443f96→868d263 (`0a64c99` homepage speed-up + `868d263` review photos shrink), **both explicitly "no engine bump, no SQL"** → jerky stays **Engine v71** and the manual stays **plain v71** (no manual rebuild needed — the bakery manual has no new content for these). Two parts: (a) the bakery moved its homepage's six inline base64 photos into `img/` files papering its 249 KB→22.7 KB page — **jerky needs nothing here**, its homepage already loads `images/*.jpg` as files and `index.html` is only ~29 KB, and it has no `img/` dir; (b) the review-photo work **does** apply: `reviews.js` (root; jerky's copy differs from the bakery base ONLY in lines 29–30 = its own Supabase url+anon, so the bakery `0443f96..HEAD` patch applied cleanly with `git apply`) — `PHOTO_MAX_SIDE` 1600→**1000** (the card renders ≤340 px tall) and the carousel no longer fetches every published photo up front: a slide's photo waits in `data-src` and `loadPhoto(node)` promotes it when that slide is shown **or the one after** (so a 6 s advance never shows a blank frame); a single published review still promotes immediately (`drawGrid` calls `loadPhoto(card)`). `store/app.js` got a **comment-only fix** (the bakery's correction of its own v71 notes): "the product photos" → "the storefront settings" in the `repaintForLang` and `setLang` comments — the shop has no product photos, so a language switch never cost images; the same factual correction was re-applied to jerky's v71 wording in `CHANGELOG.md`, `README.md`, `admin/js/views/guide.js` and the `test/store-lang-switch.test.js` header. `CHANGELOG.md` gained a localized "10 Sep 2026 — review photos load one at a time (no new engine; the phones did not change)" note (honestly noting jerky's homepage was already file-based) and `changelog.pdf` was rebuilt (5 pp); `README.md`'s reviews section documents the 1000 px shrink + the `data-src`/`loadPhoto` lazy rule. **607 tests pass** (bakery 601). Sandbox-verified on port 8462: the real single published review's photo promotes immediately; with the read stubbed to three photo reviews (no writes), the carousel fetched slides **0 and 1 only**, leaving slide 2 waiting in `data-src`, and tapping **›** (live "2 / 3") promoted slide 2 exactly as it became the next slide; reload restored the real single review; console error-free.

- **Engine synced to v73–v78** (2026-09-14) — bakery HEAD 831b1cc→024dce8 → jerky, **code-only (no SQL)**, six engine versions in one pass (36 files, +1721/−161). **v73 —** a per-product **change/cancel window** (`cancelDays`, the new "Changes or cancellations (days before delivery)" box; **advisory only** — it never blocks the baker, who moves every order by hand; blank = no window, `0` = no advance limit); **moving an order** to another posting day (pure `moveOrderGroup(group, dest)` in `state.js` + the Edit pop-up's **"Delivery day"** select of every day still to come — it writes `deliveryDateId` and the `deliveryDate` snapshot together so a split group heals, re-bases the capacity guard on the destination day, republishes the track card, and leaves the emptied source date to Deliveries' **Del**; soft notes report the window / a day inside it / a closed day / a short item, none blocking); a **Policies** box (`settings.storefront.policy`/`policyZh`/`policyMs` — typed once in English, machine-translated, drawn on the shop under Track your order in the visitor's language with `textContent` + `white-space: pre-line`, blank hides it); and app-wide **`admin/js/suggest.js`** — a greyed `data-suggest` value is taken by the right arrow or a tap on the drawn arrow, with the credential/sign-in fields deliberately carrying none (pinned by test). **v74 —** that tap no longer dies after the first field: `acceptSuggestion` no longer focuses (so no keyboard shifts the page under the thumb), the strip is `clamp(30, width*0.28, 52)`, and a tap is judged against the field's own `offsetX`/`clientWidth` **or** the page's `clientX`/rect (an OR, so it can only accept more, never refuse more), plus a delegated `click` beside `pointerdown`. **v75 —** the green hit glow on a jumped-to order now pulses (`hit-glow 1.4s ease-in-out infinite`) until the pointer reaches the row (`pointerenter`/`pointermove`/`pointerdown`/`mouseenter`/`touchstart`, self-removing listeners); a steady ring under `prefers-reduced-motion`. **v76 —** the product **translated-text card folds away** (shut by default, tap the title to open, fold on a tap outside); each empty line shows its translation as an ordinary greyed suggestion whose → swaps to a **↻** that re-translates that one line; the "Fill all" / "Translate this one" buttons and the "auto" tag are gone, and `kickoffAutoTranslate` still runs on every save/publish. **v77 —** the card's blank-line hint reads "…if blank, it will be filled with English". **v78 —** Delivery Dates relabels **"＋ Add occasion" → "＋ Load standard occasions"** and the in-window **"Add" → "Add my own day"**. Wholesale (blob == base): `admin/js/occasion_catalog.js`, `admin/js/version.js` (→78), `views/deliveries.js`, `views/suppliers.js`, `views/units.js`, `i18n.js` (+`policyFor`), `store/pool.js` (+`cancelDaysFor`/`strictestCancelDays`), the new `admin/js/suggest.js`, `test/order-move.test.js`, `test/suggest.test.js`, and four re-copied test files (`i18n`, `occasion_catalog`, `store-i18n`, `store-pool`). Patch-applied clean: `admin/js/supabase.js`, `views/ingredients.js`, `admin/css/app.css`, `store/app.css`, `store/index.html` (new hidden `#policy-box` + `#policy-text`), `store/config.js` (`tngQr` dropped), `test/{orders,products-editor,storefront.config,supabase,store.pool.dom}.test.js`. Hand-merged keeping localization: `state.js` (policy fields + `moveOrderGroup`; jerky's `postageRM`/`postageSet` lines kept between `tngQr` and `products`), `app.js` (`installSuggestionAccept`; `/guide` route intact), `views/customers.js` (pet-worded likes/avoid placeholders + `data-suggest`), `views/products.js` (wholesale bakery HEAD, then 9 jerky strings re-applied — "Feeding tip", pouch rate limits, Chicken Jerky placeholders), `views/orders.js` (`data-suggest` on both WhatsApp boxes + `productOptions(state, curId)`), `views/settings.js` (`data-suggest` + the whole Policies block + `translatePolicy`), `store-lang.js` (5 new keys × 3 languages in jerky's posting-day words: 发货日 / hari pos), `store/app.js` (the receipt's cancel line via `strictestCancelDays`; `mergeStorefront` gains policy, drops tngQr), `test/store.test.js` (localized; jerky's postal-address gate bypassed with `fulfillment._value = "collect"`), `test/store.pool.dom.test.js`, `test/suggest.test.js` ("you@example.com"). Guide screen six v73–v78 "Newest in the engine" cards; `CHANGELOG.md` → v54→v78 (six v-sections + two dated **no-engine** notes — the New Orders inbox-tap reveal and the TNG-is-messaging-only rewrite — and the last `*italic*` converted to `__italic__` so fpdf2 renders it instead of printing literal asterisks); `marketing/build_changelog.py` now reads the engine number live (banner + footer) and maps ↻; root `changelog.pdf` rebuilt (8 pp); `README.md` four new sections (change/cancel windows & moving an order, Policies, suggest.js, screen polish) plus the corrected TNG/track-page claim; marketing manual rebuilt **plain v78** (24 pp). **647 tests pass** (bakery 643). Sandbox-verified on port 8462: the shop renders, Policies stays hidden while blank, the cancel note appears on the card and the receipt, EN/中文/BM switches, no console errors.
- **Engine synced to v79–v88** (2026-09-14) — bakery HEAD 024dce8→493e6bd → jerky, **code-only (no SQL)**, ten engine versions in one pass. **The one big theme: a month calendar replaces the row of date chips on both sides of the shop, and a day you marked is drawn the same way on every calendar.** **v79 —** the customer picks a posting day on a **month calendar** under *Pick a posting day* (green ring = open, chosen day written out below as "Your posting day: Wed, 16 Sep", first open day pre-chosen); the new pure root module **`availability.js`** holds a product's sell-day rules; **`store/calendar.js`** is the shop's own copy of the grid helpers (drift-guarded by `test/store-cal.test.js`); **`admin/js/datepicker.js`** backs the app's three date fields (expands in place, because the app's pop-ups share one layer). **v80 —** the shop's holiday **wash with a tap-to-name bubble** and a **glowing today** (no more red box on a quiet day); the **Orders screen's date strip becomes the same month calendar** with booking counts (`3/12` / **FULL**, red once closed); the **＋ New order card arrives folded and leads with the calendar**. **v81–v82 —** a product's **sell days are marked on its own Availability card** (tap a weekday heading, tap a day, drag a run, stretch a period's Starts/Ends; marks never carry into another month; a product with no marks sells every day) and the shop **hides a product on a day it is not sold** (the notice period is the one exception — it stays with its note); the old From/To season boxes become one such mark on first open. **v83 —** marking a holiday **republishes the shop in the same save** (was waiting for some unrelated save). **v84–v86 —** a marked day is one **see-through wash** everywhere (depth = how long the mark runs), drawn by the new shared **`admin/js/occgrid.js`**; the Orders-screen geometry is fixed so a mark sits on its line of dates at one height. **v87 —** **tapping a marked day names it** (same bubble the shop shows) on every app calendar, still doing that day's usual job; the Order date boxes name their day above the date. **v88 —** the tint sits on the line of dates at Orders; a **mouse resting on a marked day names it** (a phone still taps). Wholesale (blob == base): `availability.js` (new), `admin/js/version.js` (→88), `admin/js/dates.js`, `admin/js/occasion_catalog.js`, `admin/js/calendar.js`, `admin/js/occgrid.js` (new), `admin/js/datepicker.js` (new), `admin/js/views/deliveries.js`, `store/pool.js`, `store/calendar.js` (new), plus 13 new/re-copied test files. Patch-applied clean: `admin/js/state.js`, `admin/js/supabase.js`, `admin/js/views/orders.js`, `store/app.css`, `store/index.html`, `store/config.js`, `test/{store-lang-switch,store.pool.dom,storefront.config,supabase}.test.js`. Hand-merged keeping localization: `admin/js/views/products.js` (the Availability card replaces the From/To pair; 9 jerky strings — "Feeding tip", Chicken Jerky placeholders, pouch wording), `admin/css/app.css` (the `.avail-*`/`.cal-*` block; "the bakery" comments → "you"), `store/app.js` (all 8 jerky deltas re-verified intact — 🐾, Post (nationwide)/Collect (local), the postal-address-required `showConfirm` gate, `fulfillment || "courier"`, reset-to-courier, `addrField.hidden = false`, "Preparing", `apply("courier")`), `store-lang.js` (6 new keys × 3 languages in jerky words — "Your posting day: %1" / 你的发货日 / Hari pos anda), `test/{store.live,store-lang-switch,store.sell,store.pool.dom}.test.js` (localized assertions only). Guide screen ten v79–v88 "Newest in the engine" cards; `CHANGELOG.md` → v54→v88 (ten v-sections); root `changelog.pdf` rebuilt (13 pp); `README.md` new "Calendars" section + file inventory (availability.js, store/calendar.js, datepicker.js, occgrid.js); marketing manual rebuilt **plain v88** (28 pp). **747 tests pass** (bakery 743). Sandbox-verified on port 8462: the shop renders the month calendar, day 16 → 18 tappable with the chosen line updating ("Your posting day: Wed, 16 Sep" → "Fri, 18 Sep"), EN→中文→BM switch in place with the localized chosen-day lines, `/admin/` boots clean to its PIN gate (the calendar screens sit behind the lock — covered by tests), no mobile overflow at 375 px, console error-free; identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`, no `jienluv2bake`/`Bakester`).
- **Engine synced to v89–v92** (2026-09-15) — bakery HEAD 493e6bd→8ca9779 → jerky, **code-only (no SQL)**, four engine versions in one pass. **v89 —** the Delivery Dates screen is **just the calendar** now: the row of date cards is gone, the month grid is the page, days in the past fold under a **Past dates** heading, and an imported occasion is removed by **tapping it** (the old ✕/Del buttons went with the cards). **v90 —** a product can **stay on the shop on a day it can't be ordered** — a new per-product **Keep it on the shop** switch (`alwaysListed`, published only when true so an old phone sees today's behaviour) keeps the item visible and marked *Unavailable* rather than hidden, with a "Next available: Fri, 18 Sep · 12 left" line; the new pure **`nextOrderable()`** in `store/pool.js` works out that next day (and a remaining count when a limit publishes one); the shop draws `.prod-next` with a gentle `next-pulse`. **v91 —** a tap on a day you don't post is **answered, not ignored**: `deliveryCal` gains a `noteMisses` option that writes a `.cal-miss` line under the grid ("Sun, 20 Sep is not a delivery day. Add it in More → Delivery Dates."), and the chosen day repaints **before** `onPick` runs so the line is never painted over. **v92 —** the Products screen's **＋ New product card arrives folded** (a module-scope `newFormOpen` flag, because the card is rebuilt on every change so a DOM node can't remember) and the Availability card's explanatory line moves under the switch. Wholesale (blob == base): `admin/js/version.js` (→92), `admin/js/views/deliveries.js`, `store/pool.js` (+`nextOrderable`), `test/cal-naming.test.js`, `test/orders-cal.test.js`, `test/store-i18n.test.js`, `test/store-pool.test.js`. Patch-applied clean: `admin/js/supabase.js` (publishes `alwaysListed`), `admin/js/views/orders.js` (`noteMisses`), `admin/js/views/products.js` (keep-listed switch + folded card), `admin/css/app.css` (`.past-head`, `.avail-listed`, `.switch*`, `.cal-miss`), `store/app.css` (`.prod-next` + `next-pulse`), `store/app.js` (imports `nextOrderable`, keeps `alwaysListed` in `mergeStorefront`, marks a kept product `unavailable` + `nextNote`), `test/{supabase,storefront.config,products-editor,store.sell}.test.js`. Hand-merged keeping localization: `store-lang.js` (3 new keys × 3 languages — "Unavailable" / 暂无供应 / Tidak tersedia, "Next available: %1" / 下次可预订：%1 / Seterusnya tersedia: %1, + the "· %2 left" form; comments reworded from "the baker" to "you"), `test/store.test.js` (the posting-day wording the bakery patch expected), `test/store.sell.test.js` ("5 days before the posting day"), `admin/css/app.css` comment ("a day you do not deliver"), `admin/js/views/products.js` commentary (9 jerky strings re-verified intact). Guide screen four v89–v92 "Newest in the engine" cards; `CHANGELOG.md` → v54→v92 (four v-sections); `marketing/build_changelog.py` `clean()` gains `−`→`-` (fpdf2 could not encode U+2212); root `changelog.pdf` rebuilt (14 pp); `README.md` new section "Delivery dates, the keep-listed switch, and folding cards (v89–v92)"; marketing manual rebuilt **plain v92** (28 pp). **765 tests pass** (bakery 761). Sandbox-verified on port 8462: the shop renders the month calendar with "Your posting day: Fri, 18 Sep", the BM switch repaints in place ("Hari pos anda: Jumaat, 18 Sep", URL unchanged), `/admin/` boots to its PIN gate, Products shows the folded "＋ New product ▸" card, and a product's Edit pop-up shows Availability "Every day · kept on the shop" with the switch `checked: true`; console error-free. Identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`, no `jienluv2bake`/`Bakester` — the one "baker" in the guide PDF is the pre-existing heritage line "(from the bakery system, September 2026)").
- **Engine synced to v93** (2026-09-16) — bakery HEAD 8ca9779→e8a2eca → jerky, **code-only (no SQL)**, one engine version. **v93 —** the WhatsApp confirmation's track link (`/store/?track=CODE`) no longer dumps the customer at the top of the page: `store/index.html` wraps the heading + card + result in a `<section id="track-section">`, and `revealTrack()` (run only when a `?track=` code is present) adds `.hit` and calls `aimAtTrack()` (`scrollIntoView({ block: "start", behavior: "smooth" })`). The glow ends on **the pointer arriving** — `TRACK_SETTLE_ON` (`pointerenter`/`pointermove`/`pointerdown`/`mouseenter`/`touchstart`, self-removing) — never on a timer, the same never-miss rule the backoffice uses when it jumps to an order. It aims **twice**: once on open, once more from the first data refresh (`trackAimPending && trackLit`) because the published config and the day's counts land later and make the page taller (measured 113 px short at 375 px); only that first pass, so the 30 s poll never pulls a customer back. `store/app.css` carries `track-glow` keyframes with a steady lit card under `prefers-reduced-motion`. Wholesale (blob == base): `admin/js/version.js` (→93), `test/store-track-jump.test.js` (new — brand-agnostic, no localization needed). Patch-applied clean: `store/app.css`. Hand-merged (patch context differed — jerky's `wireFulfillment` ends `apply("courier")` where the bakery's ends `apply("collect")`, and jerky's `trackHint` `<p>` is split over three lines where the bakery's is one): `store/app.js` (3 hunks) and `store/index.html` (the `<section>` wrapper). All 8 jerky store deltas re-verified intact. Guide screen one v93 "Newest in the engine" card; `CHANGELOG.md` → v54→v93 (one v93 section); root `changelog.pdf` rebuilt (14 pp); `README.md` new section "The track link lands on the card, lit (v93)"; marketing manual rebuilt **plain v93** (28 pp, §7 Track your order). **769 tests pass** (bakery 765). **Also corrected this pass:** the v91 `.cal-miss` line was described in three places (CLAUDE.md, the guide card, memory) as reading "You don't post on Tuesdays" — the real string, in the app and the test, is *"Sun, 20 Sep is not a delivery day. Add it in More → Delivery Dates."* (jerky's admin deliberately says *Delivery day* for the picker, e.g. the Edit pop-up's "Delivery day" select, so this is consistent, not a localization miss). Sandbox-verified on port 8462: `/store/?track=a3f9c2` opens with `#track-section` carrying `.hit`, scrolled to the card, then settles on pointer arrival; `/store/` with no query does neither; console error-free. Identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`, no `jienluv2bake`/`Bakester`) — plus one stray tidy: `supabase/availability.sql`'s header comment said "Bakester live availability tables." → "Munchies Furkidz …" (a comment only; **no SQL re-run needed**).
- **Engine synced to v94–v96** (2026-09-16) — bakery HEAD e8a2eca→be92071 → jerky, **code-only (no SQL)**, three engine versions in one pass. **v94 —** one wording change on the customer's page: a product kept on the shop (`alwaysListed`) that is not sold on the chosen posting day said *"Only sold on %1"* and now reads *"Only available on %1"* (`store-lang.js` `closedWeekday`, en/zh/ms — the Chinese *只限 %1 供应* and Malay *Hanya tersedia pada %1* already said the equivalent). **v95 —** a day's `x/y` now counts only what can really be ordered that day: new pure **`dayCapacityParts(state, dateStr, overrides)`** in `admin/js/bom.js` returns `{ parts, counted, off, total, fallback }` — it imports **`sellOpen` from `../../availability.js`** (the shared root module the shop reads, so the admin sum and the shop's own capacity are ONE computation and can never disagree), each part carries `onSale` + its effective limit, `counted` = the parts on sale, `total` = their limits summed, and `fallback` (= the Settings `defaultCapacity ?? 12`) applies only when nothing on sale that day has a limit — never 0, which would read as Sold out. `effectiveCapacity(state, dateStr)` is now a one-line wrapper over it, so every existing caller is unchanged; knock-on: a day now turns **FULL** when every *sellable* unit is booked. **v96 —** that day's pop-up shows its working: `openDayAdjustPopup` (`views/orders.js`) gained a `paintSum()` that feeds what the owner is typing into the same `dayCapacityParts` as `overrides` and draws a `.cost-grid` under the rows — a `·`/`+` row per counted product (`name (sold out)` when zero), then a `=` total row reading *"what the order page can take that day"* (or the fallback line when nothing on sale has a limit), plus a *"Not counted: … — not sold on this day"* line and a *"Booked so far: N. The order page can still take M."* line; it repaints on every `input` and runs once before the owner types. No CSS change needed — the `.cost-*` classes already exist for the product cost recipe. Wholesale (blob == base): `admin/js/version.js` (→96), `admin/js/bom.js`, `test/bom.test.js`, `test/orders-day-sum.test.js` (new). Patch-applied clean: `admin/js/views/orders.js`, `store/app.js` (comment-only: `("Only sold on Mon.")` → `("Only available on Mon.")`), `test/store.sell.test.js`. Hand-merged (patch context differed — only the comment above `closedWeekday` reads "The owner's marked sell days…" in jerky where the bakery has "The baker's…"): `store-lang.js` (3 one-line string changes). All 8 jerky store deltas re-verified intact. Guide screen three v96/v95/v94 "Newest in the engine" cards; `CHANGELOG.md` → v54→v96 (three v-sections); root `changelog.pdf` rebuilt (15 pp); `README.md` new section "A day's capacity counts what you sell that day (v94–v96)"; marketing manual rebuilt **plain v96** (29 pp) — it gained §5's "What a day's total counts (Engine v95)" sentences and the new "Changing one day's numbers - 'Set day's availability' (Engine v96)" paragraph (the manual had never documented that pop-up), and its long-stale `('Only sold on Mon')` example was corrected to `('Only available on Mon')` for v94. **775 tests pass** (bakery 771) at that point — see the bug fix below, which took it to 778.
- **Bug found while verifying v94–v96, fixed in jerky (2026-09-16) — a stray "null" printed on screen.** `replaceChildren()` is NOT `el()`: a `null` argument is stringified into a literal `"null"` text node rather than dropped, so `replaceChildren(a, cond ? b : null, c)` prints the word **null** on the page whenever `cond` is false. Two places were reachable: (a) `openDayAdjustPopup` (`admin/js/views/orders.js`, v96) printed "null" between the total row and the "Booked so far" line **whenever every product on sale that day was counted** — the common case; found in the sandbox (the "Not counted" line was absent, so the null branch was taken). (b) `renderSettings` (`admin/js/views/settings.js`) printed "null" at the very bottom under Delete all data **whenever she has any product or ingredient** (i.e. always, once set up) — the `sampleCard` variable is null then. A third, **latent** one was fixed in the same pass: `admin/js/datepicker.js`'s optional Today button (`todayShortcut` is never false today, so unreachable until a caller passes it). **Both reachable ones exist identically on the bakery** (bakery `admin/js/views/settings.js:525`, `orders.js:862`, `datepicker.js:123`) — flag them to the developer to fix upstream, and **re-apply these three edits whenever those files are next re-copied wholesale** (the fixes are a `[...].filter(Boolean)` / `...(x ? [x] : [])` around the conditional child). New guard: **`test/no-null-text.test.js`** (jerky-only, 3 tests) — it renders the day pop-up (both branches) and the Settings screen through a **strict** `replaceChildren` shim (the other test shims filter nulls, which is exactly why the defect survived this long) and fails on any `"null"`/`"undefined"` text node; verified to fail on the buggy code and pass on the fixed code. **778 tests pass**.
  Sandbox-verified on port 8462 (the whole v94–v96 pass): `/admin/` boots and "Use without cloud sync" opens the app; the Orders calendar shows `0/12` on Fri 18 Sep (Chicken Jerky is marked Mondays-only, so the day falls back to the Settings default of 12) and `0/10` on Mon 21 Sep (its own daily limit counts); "Set day's availability" opens the v96 pop-up and both branches render correctly — Fri: *"= 12 the day's default capacity (Settings) — nothing on sale this day has a daily limit"* + *"Not counted: Chicken Jerky 100g — not sold on this day…"* + *"Booked so far: 0. The order page can still take 12."*, Mon: *"· 10 Chicken Jerky 100g"* / *"= 10 what the order page can take that day"* + *"…can still take 10."*; typing `-4` repaints live (*"Today: 6"*, total 6) and `-10` shows *"(sold out)"* / *"Chicken Jerky 100g (sold out)"*; a sweep of all 13 admin screens after the fix finds no "null" text anywhere; console error-free.

- **Engine synced to v97–v98** (2026-09-16) — bakery HEAD be92071→ced525d → jerky, **NOT code-only: one SQL line**. Two engine versions in one pass, one theme: **the last order status is ONE label covering both endings — "Collected / Posted" (jerky; the bakery says "Shipped") — and a placed order gains a courier tracking number.** **v97 —** the final step stopped saying "Delivered" for everything: it is named for how the order actually left (**Collected** on a `collect` order, **Posted** on a `courier` one) in the row's status list, the row's own journey map, the day's status filter and the customer's track map; a posted order gained a **Courier tracking number** box (from Packed onwards, and in the Edit pop-up) and a **Send posted message** button — the third later-stage WhatsApp message, `buildShippedMessage` in `admin/js/messages.js`, which runs through the same `basics()` (order code, frozen sold names/prices, postage line), adds `Tracking number: …` only when there is one, and returns null without a WhatsApp number; the number is published as `tracking_no` in `trackingSnapshot` and drawn on the customer's card as `.track-no` under the delivery details (self-collect orders never show the line). **v98 —** you asked for the pair itself, so the last stage is one static label everywhere (jerky: **`["delivered", "Collected / Posted"]`**); the tracking box that sat on the row in v97 is replaced by a **Note / tracking** button beside Edit, opening `openNoteTrackingPopup` — exactly the order's Note and the Courier tracking number, saved together, republishing the card only when the number changed (`applyPopupEdits` compares `trackingBefore`). Wholesale (blob == base): `admin/js/version.js` (→98), `supabase/track_no.sql` (new), `supabase/tracking.sql`, `test/orders-day-sum.test.js`, `test/store-i18n.test.js`. Patch-applied clean: `admin/js/supabase.js`, `store/app.css`, `store/app.js` (comment-only), `test/messages.test.js`, `test/supabase.test.js`. Hand-merged (patch context differed — jerky's postage hunks, "Preparing" vs "Baked", Post/Collect wording): `admin/js/messages.js`, `admin/js/views/orders.js` (the big one — STATUSES, the Edit draft + tracking box, `applyPopupEdits`, `openNoteTrackingPopup`, `shippedMsgButton`, the ready-stage split by `fulfillment`; **the v96 `.filter(Boolean)` null fix re-verified intact**), `store-lang.js` (`trkDelivered` → **`trkFinal`** in all three languages + the new `trackingNo` key), `test/store.test.js` (journey labels, the two new v97 tests), `test/orders-view.test.js` (localized "Collected / Posted", "Send posted message"). **Localized wording chosen this pass:** "Collected / Posted" (已取货 / 已寄出 · Telah diambil / Telah dipos), "Send posted message", "Posted message drafted — press Send in WhatsApp", `trackingNo` = "Tracking number: %1" (快递单号 · Nombor penjejakan). **SQL to run once in her Supabase SQL editor: `supabase/track_no.sql`** (`alter table order_tracking add column if not exists tracking_no text;`, safe to re-run) — until it runs the number still reaches the WhatsApp message and the app, just not the customer's page. Guide screen two v98/v97 "Newest in the engine" cards; `CHANGELOG.md` → v54→v98 (two v-sections); root `changelog.pdf` rebuilt (16 pp); `README.md` new section "The last status, and a posted order's tracking number (v97–v98)" + the `track_no.sql` inventory row; marketing manual rebuilt **plain v98** (29 pp) — §7 now names the pair + the tracking line, §3/§5 the Note / tracking button and the Packed-stage split. **Bug found while verifying v97–v98, fixed in jerky (2026-09-16) — the customer's page never asked for the number.** `store/app.js`'s track lookup builds its own PostgREST URL (`/rest/v1/order_tracking?select=…`), and PostgREST returns **only the columns named in `select`**. v97 added `tracking_no` to the row and to `paintTrack()` but never added it to that `select`, so `row.tracking_no` was always `undefined` and the customer's track card could never draw the line — the whole customer-facing half of the feature was dead on **both** repos (bakery `store/app.js:1308` has the identical omission). jerky now names it (`select=…,tracking_no,updated_at`) with a comment saying why, and `test/store.test.js`'s "a posted order shows the courier's tracking number" now captures the request URL and asserts it asks for `tracking_no` (verified to fail on the old string). **Flag the bakery's copy to the developer.** Re-apply on the next wholesale re-copy of `store/app.js` — the v79–v88 sync re-copied it, and this is the second jerky-only fix in that file alongside the 8 localization deltas.

**789 tests pass** (bakery's own suite is green at this HEAD). Sandbox-verified on port 8462: the shop's track card draws `.track-no`; the admin Orders row shows the **Note / tracking** button and the last status reads **Collected / Posted**; console error-free. Identity sweep clean.

- **Engine synced to v99–v102** (2026-09-16) — bakery HEAD ced525d→0036d67 → jerky, **code-only (no SQL)**, four engine versions in one pass (the bakery shipped **v101 and v102 in a single commit** — `0036d67` — whose message names only v102, so the file set is the whole 21-file diff, not per-commit). **v99 —** the shop's **"Next available" line is a control**: tapping it takes that posting day (`store/app.js` reuses the calendar's own `pickDay(day, { scroll: true })` and scrolls the grid into view), so the product in hand can be ordered at once; with **anything already in the basket** the line keeps `.off` and the tap answers instead of acting — a new `store-lang.js` key `nextBlockedBasket` ("Your basket is for %1. To order for another day, choose it on the calendar above." / 你的购物袋是 %1 的 / Bakul anda untuk %1), because taking a day would silently move the whole basket and drop whatever no longer fits. **v100 —** More → Delivery dates gains **two more ways in**: a **weekday letter** above the grid is a button that picks/unpicks every one of that weekday in the month shown (`.cal-dow.dow-pick`), and **Generate the next dates** — which used to exist only inside Home's "no delivery dates yet" message and so deleted itself the first time it worked — is now always on that screen, following `settings.deliveryDays`; `admin/js/dates.js` gains **`dayListLabel`** so Home's empty state and the line under Generate name her own days instead of a hard-coded "Mon/Wed/Fri". **v101 —** the till: new pure **`admin/js/money.js`** (`groupValue`, `isCollected`, `methodOf`, `deliveryOf`, `paidOf`, `dayMoney`, `moneyBetween`) + new screen **`admin/js/views/money.js`** (`#/money`, More → **💰 Money**, Today / This week / This month), with the counting rule that matters — **collected money is bucketed by `paidAt`** (falling back to the delivery date for anything paid before v101) and **still-to-collect by the delivery date**; every item line on the ＋ New order form and in the Edit pop-up gains its own **price box** (seeded from the menu price, blank = the product's price — every reader already prefers the v70 frozen `unitPrice`, so nothing downstream changed); the row's Paid button splits into **Paid · Cash / Paid · TNG** (`markPaid(state, group, root, dateId, method)`, writing `paidMethod` + `paidAt`, drawn as a `.paid-tag`), and the v98 Note / tracking pop-up becomes **Note / tracking / payment** with a **Paid by** select. **v102 —** **drag-to-select** on the delivery-dates calendar (`.cal-cell.drag-sel`), the same gesture a product's sell days use; deliberately **no From/To pair** there, because a delivery date is one day where a sell period genuinely has two ends. Wholesale (blob == base): `admin/js/version.js` (→102), `admin/js/dates.js`, `admin/js/views/deliveries.js`, `admin/js/money.js` (new), `admin/js/views/money.js` (new), `test/cal-naming.test.js`, `test/orders-day-sum.test.js`, `test/money.test.js` (new — bakery fixtures kept as-is per the fixture rule). Patch-applied clean: `store/app.css`, `admin/js/views/dashboard.js`, `test/multiadd.test.js`, `test/orders-view.test.js`, `test/store.sell.test.js`. Hand-applied keeping localization: `admin/css/app.css` (three blocks — `.paid-tag`/`.paid-tag.tng`, `.cal-dow.dow-pick`, `.cal-cell.drag-sel`; the bakery patch had failed on a "a day the bakery does not deliver" comment), `admin/js/app.js` (the `renderMoney` import + the `/money` route beside `/deliveries`), `admin/js/views/more.js` (the 💰 Money row between Suppliers and Delivery dates), `admin/js/views/orders.js` (**the big one** — `git apply --reject` landed hunks 4–15 on a 5-line offset but **silently dropped hunks 16 and 17** and wrote **no `.rej` file**, so both were hand-added: the **Paid by** select in `openNoteTrackingPopup` and the **Paid · Cash / Paid · TNG** row buttons; a comment pair the earlier sync had lost was restored and localized to "Preparing"; the v96 `.filter(Boolean)` null fix re-verified intact), `store-lang.js` (one key × 3 languages, above). **On `nextBlockedBasket`:** the bakery's own copy mixes vocabulary — this key says *basket* / 购物袋 / *Bakul* while its `confirmChangedBody` says *cart* / 购物车 / *troli* — so jerky mirrors the bakery exactly rather than inventing a house word; the first port had drifted to "cart" and `test/store.sell.test.js` caught it. Guide screen four v99–v102 "Newest in the engine" cards (the **Guide screen is jerky-only** — the bakery has no `/guide` route or `views/guide.js`, so these cards have no upstream source); `CHANGELOG.md` → v54→v102 (four v-sections, localized); root `changelog.pdf` rebuilt (17 pp); `README.md` new section "The till, a tappable Next-available line, and picking dates fast (v99–v102)" + the `admin/js/money.js` inventory row; marketing manual rebuilt **plain v102** (29 pp — its Home paragraph, the Availability card's Next-available line, the More-screen list and the Delivery-dates bullet all updated). **805 tests pass** (bakery 798). Sandbox-verified on port 8462: `/store/` renders with the tappable Next-available line in both branches, More shows **💰 Money**, the Money screen renders Today / This week / This month, the delivery-dates calendar shows the weekday letters and drag-select; console error-free. Identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`, no `jienluv2bake`/`Bakester` in source).

- **Engine synced to v103–v113** (2026-09-17) — bakery HEAD 0036d67→5da2347 → jerky, **code-only (no SQL)**, eleven engine versions in one pass. **The one big theme: the app keeps books.** **v103 —** money that leaves: `state.expenses[]` (`kind` = a category, `paidFrom` = a pocket), the ＋ Add an expense card, and `askWhatYouPaid` so the ＋ New order form can ask what a packet really cost. **v104 —** money that comes in besides sales: `state.deposits[]` and **Put money in** — the other side of a pocket, for stock bought from her own purse before any orders came in, or a float of change; `state.pockets` is derived. **v105 —** the new pure **`admin/js/profit.js`** (`orderDay`, `lineCost`, `profitBetween`, `monthSpan`, `expenseRows`) and the new **Profit** screen (`#/profit`, More → **📈 Profit**, Today / This week / This month / a chosen month): sales − cost of the treats sold − running costs, with capital and drawings kept out of the profit line. **v106 —** the note field, and **her own categories and ways to pay** (`state.categories[]` / `state.payMethods[]`) edited in **`admin/js/views/accountsEditor.js`**. **v107 —** the two-line order item line (what was sold, then what it cost). **v108 —** those two lists move under Money (**Categories & ways to pay**). **v109 —** per-method **journals** that expand in place, plus a **TNG `methodLabel` bug fix**. **v110 —** an ingredient can be **Not something I buy** (`notPurchased`, a cost in your recipes that never reaches a shopping list or a PO; `purchasing.js` `notPurchasedNames`). **v111 —** a tap on a journal line opens that book. **v112 —** **Pay back a pocket** — a "My own withdrawal" (drawings) that pays a pocket down (`state.pockets`), and the category **pill rows wrap** instead of overflowing. **v113 —** the **Books** line on the Money screen (`openBooks`) listing each pocket with its balance and its journal. New pure modules: **`admin/js/accounts.js`** (the categories/ways-to-pay readers + built-in defaults), **`admin/js/profit.js`**. Copy-only relabels and comment fixes this pass: `admin/css/app.css` (two "the baker" comments → "you"), `admin/js/views/orders.js` ("Add more only if she can bake extra" → "…if you can make extra"), `admin/js/app.js` ("tell the baker's helper this message" → "tell the developer this message"), `admin/js/views/money.js` (a flour comment → "A packet of meat / paid from your own purse"), plus the already-localized `profit.js` / `money.js` / `views/{profit,money,accountsEditor}.js` text. Wholesale (blob == base): `admin/js/money.js`, `admin/js/purchasing.js`, `admin/js/version.js` (→**113**), `admin/js/views/money.js`, `test/history.test.js`, `test/ingredients-editor.test.js`, `test/money.test.js`, `test/purchasing.test.js`, plus the new `admin/js/accounts.js`, `admin/js/profit.js`, `admin/js/views/accountsEditor.js`, `admin/js/views/profit.js`, `test/money-lists.test.js`, `test/profit.test.js`. Patch-applied clean: `admin/css/app.css`, `admin/js/backups.js`, `admin/js/views/history.js`, `admin/js/views/ingredients.js`, `admin/js/views/orders.js`, `admin/js/views/po.js`, `admin/js/views/settings.js`, `test/multiadd.test.js`, `test/po.test.js`. Hand-applied (patch context differed): `admin/js/app.js` (the `renderProfit` import + the `/profit` route), `admin/js/views/more.js` (the 📈 Profit row after 💰 Money), `admin/js/state.js` (`categories: []` / `payMethods: []` / `expenses: []` / `deposits: []` + the `normalize()` guards), `admin/js/sync.js` (`expenses:` / `deposits:` in `LISTS`). All three null fixes re-verified intact (`views/orders.js` `.filter(Boolean)`, `views/settings.js` `...(sampleCard ? [sampleCard] : [])`, `datepicker.js` `todayShortcut`). **A known bakery gap ported faithfully (flag upstream):** `state.js`'s new comment says `categories`/`payMethods` are "shared between phones", but `sync.js`'s `recordPayload("settings", …)` whitelist omits them — so today they are **phone-local** in both repos; her two phones would not see each other's categories. Guide screen eleven v103–v113 "Newest in the engine" cards; `CHANGELOG.md` → v54→v113 (eleven v-sections, localized to jerky words — "packet of meat", "the treats made from it", "posting day", no Kean/Suan); `marketing/build_changelog.py` `clean()` gained `.replace("✕", "x")` (the new text's U+2715 is outside latin-1); root `changelog.pdf` rebuilt (21 pp); `README.md` new section "The books: money out, profit and loss, and your own money (v103–v113)" + inventory rows for `js/profit.js` and `js/accounts.js`; marketing manual rebuilt **plain v113** (30 pp — the Money bullet, the More layout line now naming **Money, Profit**, and the Ingredients bullet's **Not something I buy** switch). **862 tests pass** (bakery 855). Sandbox-verified on port 8462 (cloud off, isolated origin): `/admin/` boots; Money renders ＋ Put money in / ＋ Pay back a pocket / ＋ Add an expense / **Categories & ways to pay · 11 categories · Cash, TNG, Loan** and the new **Books** line, which opens per-method books that expand in place; Profit renders with the localized footnote; More shows **Engine v113** + 💰 Money + 📈 Profit; the Ingredients **Not something I buy** switch renders on both the New and Edit forms, saves `notPurchased: true`, and the card then reads *"Not bought — a cost in your recipes, never on a shopping list"*; at 375 px the 12-pill category row wraps (`clientWidth 315 == scrollWidth 315`, 6 lines, `docOverflow: 0`); console error-free. Identity sweep clean. **Not ported (flagged, not invented):** the bakery's brand-new **`marketing/build_money_guide.py` → `money-and-profit-guide.pdf`** is a separate bakery artifact with no jerky equivalent — offered to the owner rather than built unasked.
- **Engine synced to v114–v115** (2026-09-17) — bakery HEAD 5da2347→c4d07f6 → jerky, **code-only (no SQL)**, two engine versions in one pass, both follow-ups *to the Profit screen only* (the books theme continued). **v114 — every spending line in Profit opens, and they're finger-sized.** On More → Profit the lines that opened their journal were only the ones with money in them; a line reading `0.00` was deliberately left dead (`opens` passed as `null`) because there was nothing behind it — but it looked identical to a live one, so in a month where most categories sit at 0.00 nearly every line tapped did nothing. `admin/js/views/profit.js` now passes the opener **unconditionally** for every spending line (Packaging, Utilities, Delivery & fuel, her own categories, and **Total expenses**), and the empty journal opens with a real sentence — *"Nothing recorded under Utilities in September 2026."* — plus its In / Out / Net at zero, instead of a dead line. The statement's own totals (Sales, Cost of sales, Gross profit, Net profit) stay figures, not doors. Two riders: the tappable rows were only **17 px** tall (a miss between two lines hit nothing) — `admin/css/app.css` adds `.info-row.tappable { min-height: 36px; align-items: center; }`; and the **Total expenses** journal now names each row's category (`whatOf(r)` → *"2 Sep · Packaging — boxes · Cash"*), because that one mixes categories, while a single category's journal still reads short (*"2 Sep · boxes · Cash"*) since its title already says which one it is. **v115 — the Profit month arrows work both ways again.** `canNext`/`now` were computed **once** when the screen was built and then reused on every step, so after stepping back a month the forward arrow stayed switched off and the only way back was to leave and re-open the screen ("one-way traffic"); `draw` now reads `const now = currentMonth(); const canNext = shown.year < now.year || (shown.year === now.year && shown.month < now.month);` **fresh on every draw**. The other calendars (Orders, Delivery dates, the date pickers) were never affected — they recompute their arrows each time they draw. Wholesale (blob == base): `admin/js/version.js` (→**115**), `test/profit.test.js` (jerky's blob had equalled the bakery's `5da2347` blob, so nothing jerky-localized was lost). Patch-applied: `admin/js/views/profit.js` (the only diff vs bakery HEAD is the pre-existing localized footnote — "the treats made from it are sold … the day you post" vs the bakery's bread/deliver wording), `admin/css/app.css` (the `.info-row.tappable` block; 55-line diff = pre-existing localization only). Guide screen two v115/v114 "Newest in the engine" cards; `CHANGELOG.md` → v54→**v115** (two v-sections, localized); root `changelog.pdf` rebuilt (22 pp); `README.md` section heading now "The books: money out, profit and loss, and your own money (v103–v115)" + two new bullets; marketing manual rebuilt **plain v115** (30 pp — the Money bullet gained the always-opens / empty-says-so / finger-sized / category-named-Total sentences). **865 tests pass** (bakery 855). Sandbox-verified on port 8462 (cloud off, isolated origin, PIN gate bypassed): `/admin/#/profit` shows every spending line tappable (`cursor: pointer`) **including RM 0.00**, all tappable rows ≥36 px, and the statement totals correctly **not** tappable; "Rent" (0.00) opens *"Nothing recorded under Rent in September 2026."* + *"It will fill up on its own as you record spending under this category on the Money screen (Add an expense)"*; the **Total expenses** journal reads *"3 Sep · Packaging — boxes · Cash"* while the single-category **Packaging** journal stays short (*"3 Sep · boxes · Cash"*); the **v115** round-trip works — September (› greyed) → ‹ → August (› live) → › → September (› greyed again), and a second › does nothing; console error-free. Identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`; `jienluv2bake`/`Bakester` appear only in CLAUDE.md/README.md as documented heritage, never in shipped source).

- **Engine synced to v116–v118** (2026-09-17) — bakery HEAD c4d07f6→9c58228 → jerky, **code-only (no SQL)**, three engine versions in one pass, one theme: **the money stage when a regular pays at pickup, plus a Day one opening balance.** **v116 —** a new **Day one** form for the day the books begin: `openDayOne(state, redraw)` in `admin/js/views/money.js`, reached from a **Day one** line with a **Set** button on the Money screen. It asks for the cash in the tin, the money on the phone, the date the books begin, and one box per ingredient for what is already on the shelf. The tin and the phone are written as ordinary `state.deposits` rows (so the Money screen's net is right from the first day and nothing new has to understand them); each stock box sets `ingredient.onHand`, which the shopping lists already subtract. Boxes are typed in the unit she keeps that ingredient in — two helpers were exported from `admin/js/views/ingredients.js` for that, the new `currentUomId(state, ing)` and the already-exported `cookingFamilyOf(...)`, so the form offers the units of the same family and converts with the ingredient's own `toBase`. Every box is validated **before** anything is written, so a bad value half-way down saves nothing at all, and an empty box is skipped entirely (reopening the form to fix one figure never wipes the rest). Ingredients marked **not purchased** are left out — they have no shelf. **v117 —** a regular who pays when she collects (cash or TNG at the counter) never passes through Paid, so the row's map was claiming money that had not been taken: it showed **Paid with a green tick**. v117 dropped the Paid step from that order's journey and kept the **Paid · Cash / Paid · TNG** buttons on at **every stage from Paid onwards** — Paid, Preparing, Packed, the last step — until the money is recorded; marking an order paid never drags it backwards, and it is stamped with the day the money landed (the day the Money screen counts it). **v118 —** that dropped step was replaced, **on the owner's instruction**, with a step that **keeps its place and wears an X**: `journeyMarks` now returns `"skipped"` for the Paid slot (`paidSkipped = !paidDone && at > PAID_AT`), drawn as an amber `.oj-cross` ✕ / `.tj-cross` ✕ in `admin/css/app.css` and `store/app.css`. Every order's map is **six steps in the same places**, so a step deliberately gone past but not paid is distinguishable from one not yet reached (grey) and from one done (green) — and `store/app.js` draws the identical six steps on the customer's track page. The status dropdown also gates on `STAGES_AT_OR_PAST_PAID`: picking Paid **or any later stage** on an order with no recorded payment marks it as owing money. Wholesale (blob == base): `admin/js/version.js` (→**118**), `test/money.test.js`. Patch-applied clean: `admin/css/app.css` (`.oj-step.skipped` / `.oj-cross` / `.oj-step.skipped .oj-label`), `store/app.css` (`.tj-step.skipped` / `.tj-cross` / `.tj-step.skipped .tj-label`), `admin/js/views/ingredients.js` (`currentUomId` + the re-export of `cookingFamilyOf`), `admin/js/views/money.js` (`openDayOne` + the Day one card-row), `test/orders.test.js`, `test/orders-view.test.js`, `test/store.test.js`. Hand-merged (patch context differed — jerky's `STATUSES` last entry is `["delivered", "Collected / Posted"]` where the bakery has "Collected / Shipped", and its stage names are "Preparing" not "Baked"): `admin/js/views/orders.js` (5 edits — `PAID_AT` + `STAGES_AT_OR_PAST_PAID` after `STATUSES`; `journeyMarks` rewritten to the `paidSkipped` / `done` array / `live` flag with the `"skipped"` return; the `oj-cross` branch in `orderJourneyEl`; the status-select handler gated on `STAGES_AT_OR_PAST_PAID`; the Paid buttons moved out of the `paid` branch into a shared `atOrPastPaid` block) and `store/app.js` (the same `PAID_AT` + `paidSkipped` / `done` / `live` rewrite in `journeyEl`, with the bakery's comment reworded "as the baker's" → "as yours"). **The three `.filter(Boolean)` null fixes were re-verified intact** after the `orders.js` edit, and so was the `tracking_no` column in the track lookup's `select` after the `store/app.js` edit. **Localized this pass:** the new `Day one` copy's "a loan for the oven" → "a loan you took out for equipment"; the new test/comment wording "Baked" → "Preparing", "Collected / Shipped" → "Collected / Posted", "the baker's" → "yours", and the new `test/store.test.js` fixtures to jerky's own ("4 Sep · Post (nationwide)", "Chicken Jerky ×2"). Guide screen three v118/v117/v116 "Newest in the engine" cards; `CHANGELOG.md` → v54→**v118** (three v-sections, localized); root `changelog.pdf` rebuilt (23 pp); `README.md` section heading now "The books: money out, profit and loss, and your own money (v103–v118)" + three new bullets (Day one; the paid-at-pickup step); marketing manual rebuilt **plain v118** (31 pp — §3's order-row paragraph gained the six-dots/amber-X explanation, §7's track paragraph the same, and the Money paragraph the stay-on buttons + the Day one sentence). **872 tests pass** (bakery 865). Sandbox-verified on port 8462 (cloud off, isolated origin, own seeded data — no reads or writes to her real Supabase): an order sitting on **Preparing** with no payment recorded draws **six** steps with an amber **✕** on Paid (`rgb(201,100,31)` on the `#fbe7d8` node, the label tinted `rgb(168,101,54)`), and the **Paid · Cash / Paid · TNG** buttons are on at that stage; pressing **Paid · Cash** records `paidReceived: true` + `paidMethod: "cash"` + a `paidAt` stamp **without moving the order off Preparing**, and the ✕ becomes the green ✓ in the same spot — the same at the last stage (all six steps, ✕ on Paid, the rest green). `/store/?track=A3F9C2` (read stubbed) draws the identical six steps, `tj-step skipped` on Paid, and the tick returns once `paid_received` is true; the track request still asks for `tracking_no`. More → **Money** → **Day one** → **Set** opens the form: Cash in your tin / Money on your phone (TNG) / the day the books begin, then one row per ingredient in its own unit (Chicken breast in kg, Salt in g, and **Labour left out** as not-purchased); a valid save wrote two `deposits` rows and set Chicken breast `onHand` to 2000 (2 kg → g), and a negative stock value wrote **nothing** at all, kept the pop-up open and toasted *"Check these: Chicken breast"*. The engine pill and the Guide screen both read **v118**, with the three new cards newest-first. No page overflow at 375 px (`.oj` 315 == 315), no stray "null"/"undefined" text node anywhere, console error-free. Identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`, no `jienluv2bake`/`Bakester` in shipped source).
- **Engine synced to v119–v123** (2026-09-18) — bakery HEAD 9c58228→ac54ef4 → jerky, **code-only (no SQL)**, five engine versions in one pass, one theme: **the customer and the product list in the order form.** **v119 —** typing a name in **either** order form offers the people you have already served: new `customerSuggester` / `suggestionNumber` / `suggestionSub` in `admin/js/views/orders.js`, a `.sugg-panel` in the normal flow (never a floating overlay — the Edit pop-up's body scrolls and would clip one), built once on the first keystroke that could show anything. Two letters is the floor (the shop's finders use the same), up to five rows, each reading *number · N orders · usually <product>*; a tap fills the **name and the WhatsApp number** and leaves the delivery day, the items and collect/courier yours. **v120 — one customer, one row:** a person is now keyed by the **digits** of their number (`phoneDigits` / `waKey`), so `+60123456789`, `60123456789` and `012-345 6789` are one person; a one-time self-join **`canonicaliseCustomers`** runs on open (`admin/js/app.js` `migratedV120`) and `collapseByKey` / `foldProfileInto` / `touchedAt` fold the duplicates into the later-touched record without losing what either knew, and a **Join with another customer** button on the customer card (`views/customers.js` `joinCustomerPopup` → `confirmJoin` → `mergeCustomers`) moves the duplicate's orders under the kept name and number after a confirm that names both people. **v121 —** `productOptions(state, dateId, excludeOrderId)` sorts and colour-tones the product list into **three kinds** — On the shop / sold out / taken down (`TONE_RANK`, `TONE_SECTION`) — and the **closed box wears the tone** (`select.tone-ok` / `.tone-warn` / `.tone-off`). **v122 —** those kinds become real headings *inside* the open list (`<optgroup class="tone-*">`), tinted on phones under `@supports (appearance: base-select)` in `admin/css/app.css`. **v123 —** the middle section is renamed **Unavailable** and holds **both** kinds together — a product that has sold out for that day, and one the shop does not sell that day at all — and an off-day product is named by the days it IS sold (**`<name> — only <days>`**, via `availSummary` from `availability.js`) rather than given a count for a day it was never on; the sold-out check deliberately comes **after** the sell-day check, because an off-day product's remaining can read 0 and would mislabel it "sold out". Two comments record the owner's own reasoning (18 Sep 2026): *"say everyday the kitchen will produce 12 packets, but day before 6pm order will closed but not necessarily the 12 will be sell off... what I want is some guide when I sell the product. Flexible but not too much."* — i.e. the picker is a guide while you sell, never a gate, and a product's shop close-days are deliberately **not** asked, since that deadline stops a stranger ordering and says nothing about what you may sell by hand. Wholesale (blob == bakery HEAD): `admin/js/customers.js`, `admin/js/version.js` (→**123**), `test/customer-suggest.test.js` (new). Localization-only diffs (verified file-by-file against `ac54ef4`): `admin/js/views/orders.js` (163 lines — "she/her/the baker" comments → "you", "Baked"→"Preparing", "Shipped"→"Posted", "Courier"/"Self collect"→"Post (nationwide)"/"Collect (local)", "focaccia"→"packets"), `admin/js/profiles.js` (20), `admin/js/views/customers.js` (34 — 🐕→🐾, pet wording, the localized `Post`/`Collect` history line, CSV filename), `admin/css/app.css` (57 — comments + jerky's pre-existing block order for `.engine-pill`/`.hol-list--scroll`), `admin/js/app.js` (10 — Munchies Furkidz titles, "tell the developer", the jerky-only `/guide` route), `admin/js/supabase.js` (2), `admin/js/ui.js` (2 — the empty-state icon 🍞→🐾). **Test comments are deliberately NOT localized:** a measurement found **201 pre-existing** bakery-voice hits (`she`/`her`/`baker`) in already-committed jerky test files, so that is already this repo's house style — only user-visible strings, assertions and the fixture policy were touched. **Bakery upstream defect found and repaired in jerky (flag to the developer):** the bakery's v119 commit **deleted three lines** of the manual-card doc comment in `admin/js/views/orders.js` and jammed the new suggester comment onto the dangling "…(a shared group), the" — jerky restored the lost lines (checked against `git show 9c58228:admin/js/views/orders.js`) and split the suggester paragraph out. **Nothing was taken from the bakery's new `supabase/order_alerts.sql`** (359 lines): it is an upgrade of the bakery's *own already-installed* alert system (carry the existing channel name out of the old function into `order_alert_topic`, plus the structured-publish formatting rule), whereas jerky's script already stores the topic in that table and already posts to the **base** `https://ntfy.sh` url with the topic inside a JSON body and `Content-Type: application/json` — verified line by line, so **no port and no SQL re-run**. The three jerky-only null fixes (`.filter(Boolean)` in `views/orders.js`, `...(sampleCard ? [sampleCard] : [])` in `views/settings.js`, `todayShortcut` in `datepicker.js`) and the `tracking_no` column in the track lookup's `select` (`store/app.js`) were all re-verified intact after the re-copy. Guide screen five v123→v119 "Newest in the engine" cards; `CHANGELOG.md` → v54→**v123** (five v-sections, localized); root `changelog.pdf` rebuilt (26 pp); `README.md` new section "The customer and the product list (v119–v123)" + the `js/profiles.js` inventory row; marketing manual rebuilt **plain v123** (31 pp — the step-5 product-list block, "The name box remembers people for you", and the digits/one-time-tidy/**Join with another customer** sentences), all five docs refreshed with `python3 marketing/build_all.py`. **910 tests pass** (bakery 903). Sandbox-verified on port 8462 (cloud off, isolated throwaway origin and seeded data — no reads or writes to her real Supabase): on Mon 21 Sep the product dropdown shows exactly three tinted `<optgroup>`s — **On the shop** (`Chicken Jerky 100g — 10 left`), **Unavailable** (`Duck Jerky 100g — only Sat & Sun` *and* `Beef Jerky 100g — sold out`), **Taken down** (`Sweet Potato Chews (hidden)`) — with the closed box computing `rgb(231,242,232)`/`rgb(63,125,78)` for ok, `rgb(253,241,220)`/`rgb(178,106,22)` for warn, `rgb(241,233,223)`/`rgb(154,132,113)` for off, and the headings bold (600); typing `ai` in ＋ New order paints `Aisyah` / `60123456789 · 3 orders · usually Chicken Jerky 100g` and a tap fills the name and number while collect/courier stays put, and the Edit pop-up shows `Bala` / `60127778888 · 1 order · usually Duck Jerky 100g`; the v120 tidy collapsed two duplicate Aisyah records on boot (`key` → `60123456789`, keeping `avoid: "onion"` from the later record and folding in `🐾 Milo`), and **Join with another customer** ran end to end — the confirm named both people, the absorbed order's `customerName`/`whatsapp` were rewritten to the kept ones and the duplicate row went, then the suggester read "3 orders"; no "null"/"undefined"/"NaN" text on any of 14 admin screens; no overflow at 375 px (client 375 == scroll 375 on orders, orders+form, customers, guide, money); console error-free. Identity sweep clean (`CNAME` `munchies.com.my`, only `ircwozniiyywsowamixy.supabase.co`, no `jienluv2bake`/`Bakester` in shipped source).

- **Sales codes & QR labels: shops, promotions, bring-a-friend** (2026-09-20) — **built HERE, so `admin/js/version.js` deliberately does NOT move (stays v123)** and the marketing manual goes to **`LETTER="c"`** → plain **v123c** (three same-day revisions: the feature itself — v123a; her "no way to select landing pages?" follow-up with its two sandbox-found fixes — v123b; then the named-pages library that follow-up turned out to be asking for — v123c, described in the last paragraph of this bullet). The engine number describes the *shared* engine, and the Guide screen's own "Engine version" row promises both apps show the same one — bumping here would make that row lie and would collide with the bakery's next sync (which is picked by blob hash, never by number). Implements `~/Downloads/Munchies_FurKidz_Reseller_Marketing_Plan.md`: a pet shop hands out a sample whose card carries a QR; the customer scans, lands on a branded page, gets an offer, and orders. **A QR is deliberately NOT single-purpose** (her explicit ask) — the same mechanism carries a product promotion ("not only thru WhatsApp link as offered by our already build in APP"), a bring-a-friend introduction and a plain hello, so a new idea is a new *code*, not an app change. Every printed label also carries a tiny human-readable code (`K3X9`, no `0/1/I/L/O`) beside the square so two labels can be told apart by eye. **New files:** `admin/js/qr.js` (hand-written, dependency-free encoder — `qrMatrix(text)` byte-mode ECC-M v1–10, `qrSvg(matrix)` → **string**, `qrPngBytes(matrix)` → `Uint8Array`; every canvas/blob call sits behind a click handler, so the ~29 test files that each re-define `createEl` inline are never asked to draw one), `admin/js/codes.js` (KINDS `shop`/`promo`/`intro`/`plain`, `makeCode`, `tasterUrl`/`shopUrl`/`labelUrl` (`labelUrl` adds `&via=` for an `intro` code only → the existing `?via=` credit engine fires **unchanged** via `order.referredBy`), `offerLine`/`offerText`/`offerMinText`, `codeStats`/`codeCustomers`/`sheetLabels`, `visitTally`, and the published half `publishCodes`/`publishTaster`), `admin/js/views/codes.js` (`#/codes`, More → **🏪 Shops & codes**: the landing copy card, the shops, the codes with QR preview + PNG download + print sheet + counts, the **Label visits** card, and a **📷 Scan a label** button), `taster/` (the landing page: `index.html`/`app.js`/`app.css`) + root `taster-lang.js`, and `supabase/taster_visits.sql`. **Edits:** `state.js` (`partners[]`/`codes[]`/`settings.taster` in `defaultState` **and `normalize()`** — miss the latter and they are dropped on load — plus `isNewCustomer(state, group)` generalising `referralFlag`: "new" = a WhatsApp number that has never bought, exactly her definition), `sync.js` (`LISTS` + the gated `taster` settings entry), `supabase.js` (`storefrontPayload` publishes codes/taster; `importIncoming` carries **`promoCode` + `codeKind`** — the stamp is silently dropped without this; `pullVisits`), `store/app.js` (`parseCode`/`currentCode`/`codeInfo`, `renderCodeBanner`, and the order stamp **only when the code really is one the app published** — a made-up `?c=` must not land in the books as a label that never existed; only code + kind travel, the shop is read back from the record), `store/index.html` (`#code-banner`), `store-lang.js` (`codeOff`/`codeMin`/`codeNew`/`codeUntil`/`codeFrom` ×3), `admin/css/app.css`, `admin/css/print.css` (`body.codes-print`), `app.js` + `views/more.js` (route + More row), `views/guide.js`, `CHANGELOG.md`, `README.md`. **No SQL is needed for the order stamp**: `incoming_orders` is `(id, data text, status, created_at)` and the whole order rides as one JSON blob. **The one SQL she must run: `supabase/taster_visits.sql`** — one small table with **RLS on, an anon INSERT policy only and an authenticated SELECT policy only**; that asymmetry is the whole privacy story (the public page can add a visit, nobody anonymous can read one back; the admin sends a Bearer token via `reviewAuth`). Until it runs, labels print and scan normally and only the counts are missing — the card names the missing SQL rather than going blank. **The money stays hers, deliberately:** the shop never computes a discount (`lines[].price` is frozen and stamped as `unitPrice`, which Money and Profit read) — a silent storefront discount would rewrite recorded revenue and profit — so the page and the banner *state* the offer and the admin *tells her what to apply* when she confirms, exactly how the referral credit already works. **Scanning** offers the camera only where `navigator.mediaDevices.getUserMedia` **and** `"BarcodeDetector" in window` (Chrome/Edge on Android and desktop — **not iPhone Safari**), so the typed box is always present beside it, never a fallback to find; `showPopup` has no on-close hook, so the camera loop detects its own teardown by polling `video.isConnected`. **One label's own words on the page** (second same-day revision, `LETTER="b"` → **v123b**; still no engine bump) — her ask, "no way to select landing pages?": the per-code `headline` box was collected, published and whitelisted into the shop's code rows but **rendered nowhere** (a genuine defect — it is what prompted the question), and there was only ever one landing page for it to mean anything on. Rather than a list of named landing pages (a new list to manage, and one shared edit silently moves every code sitting on it) that revision shipped **per-code landing lines that fall back to the shared page** — and she came back and asked for the list anyway (see the third revision below), so that first cut is now the *bottom* layer of three rather than the whole answer. In it a label may carry its own `heading`/`body` (+`Zh`/`Ms`), and anything blank resolves to the shared page's line **for that same language**, so a code with only English typed still reads Chinese from the shared page instead of blanking or mixing. **The mechanism is one shape, published twice:** the private `pageLines(src, out)` in `admin/js/codes.js` builds those six keys and is now used by `publishTaster` **and** `publishCodes` (replacing the `headline` block), and `codeCopy(shared, info)` in `taster/app.js` lays a code's non-blank lines over the shared copy before the existing `copyFor` runs — a pure function returning a fresh object, so the shared copy is never written through. The admin's side is one `copyLine(target, en, label, max)` over a **copy target** `{obj, shared, persist, redraw}` so the shared card and the code pop-up share every box, `is-mine` mark and `↻` (`sharedHint` greys the line a customer would get into each empty box; `persist` is a **no-op inside the pop-up**, whose Save is the only writer). `store/app.js`'s merge whitelist drops `"headline"` for those six keys. Two things bit while building it and are pinned by comments: `saveIt` **deletes** a blank copy key from the new row *and* from the record it replaces (`Object.assign` only adds or overwrites, so a line she deleted would stay on the label and still publish), and the pop-up's `redraw` is `() => { keepTyped(); refresh(); }` — **`keepTyped()` first**, because `refresh()` rebuilds the label/code boxes from `draft` and would otherwise revert a label she had just typed. Clearing a translated box calls `markManual` (not `markAuto`), because on a label a blank box means *"the shared page says this line"* — a decision, so `Fill 中文 / BM` must not fill it back. **Two defects the sandbox found while verifying it, both fixed here in the same revision:** (a) **the pop-up destroyed the label's override list on every edit.** `trOverride` is an **array** of variant names and the draft was copied with `{ ...(code.trOverride || {}) }` → `{"0":"headingZh","1":"headingMs"}`; `isOverridden()` only reads an array, so **every box she had typed by hand silently read as machine text again and the next `Fill 中文 / BM` overwrote her words** (seen live: a save reduced `trOverride` from `["headingZh","headingMs"]` to `["bodyZh"]`). The draft is now built by a new **exported pure `codeDraft(code, state, t, today)`** that copies `Array.isArray(code.trOverride) ? [...code.trOverride] : []` alongside the offer and `trSrc`, so nothing typed in the pop-up and then abandoned can mark the record underneath. (b) **the 中文 and BM boxes were indistinguishable**: on a label the placeholder is the *shared page's line in that language*, so a line whose shared translation is blank left both boxes showing the same English. Every translated box now carries an always-visible **`.qr-trans-lang` tag** (`中文` / `BM`), the same reason the products editor prints a `LANG_LABEL`. **New tests (115):** `test/qr.test.js` (frozen golden matrices + structural invariants + Reed–Solomon known-answer vectors), `test/codes.test.js`, `test/codes-lib.test.js`, `test/codes-draft.test.js` (new, 5 — the array-safe copy, a typed box surviving a sibling edit, nothing reaching the record before Save, an empty and a wrong-shaped list, a new label's code + default offer), `test/taster.test.js` (15 — address bar, `via`, `storeLink`, `mergeTaster`, `copyFor`, `findCode`, `offerWords` in en/zh/ms, `visitPayload`, `createView`), `test/store-code.test.js`, plus storefront/supabase/sync cases. **1029 tests pass.** Sandbox-verified on port 8462 (isolated throwaway origin; `fetch` stubbed after load wherever a real write would otherwise have reached her Supabase): the live promo label renders heading from published copy + `10% off · on RM30 and above · new customers only · valid until 30 Sep`; an expired code shows `This offer has finished` in the muted style; shop → `from Paw Shop`; intro → `A friend sent you here`; plain → the whole card hidden; 中文 and BM fully translated; two dog clicks on a clean single-boot page wrote **exactly one** row (`v.recorded` guard); `pullVisits` renders every path (404/400 → the SQL hint, 500, offline, no cloud, rows, the 2000-row cap); the admin card read `9 visits · 4 dogs · 3 cats · 2 did not say` sorted by label and counted an unmatched code in the total only; Scan opened with the camera offer where supported plus the always-present typed box; no junk text nodes, no overflow at 375 px, console error-free. **The landing-page half of the per-label-copy revision was verified on its own port (8463) through a new sandbox harness, `.claude/serve-landing-stub.mjs`** (gitignored, like `serve.mjs`): it serves the working tree unchanged, rewrites only `/store/config.js`'s Supabase url to point at itself, and answers `storefront_config` with the payload the app *itself* produced (`syncStorefront` driven in node against seeded data, so the shape is the real one — `codes[].heading/Zh/Ms/body/Zh/Ms` + `productName` + `offer`) and `taster_visits` with a 201 plus a local log. That is what let `loadPublished()` run **unmodified** without one request reaching her project. Verified: `/taster/?c=MILO` renders the label's **own** heading and paragraph ("New here?" / "Say hi at the counter") over the shared page's copy, with `from Paw Shop` and the `?c=MILO` hand-off; 中文 shows the label's own `我的标题` and its English `body` (the label overrode the body, so only its translation is missing — *a blank reflects the missing line, never the missing language*); BM shows `Baru di sini?` / `Ucap helo di kaunter`; a second code written **only in 中文** shows the shared page's heading/body in EN **and** BM (the shared copy has no BM line either, so it falls back to shared English) and its own two lines in 中文 — per-language fallback, each language on its own; an unknown `?c=ZZZZ` hides the label card, shows the shared copy and still hands the code on (the shop refuses to stamp one it never published); plain `/taster/` has no card and a bare `../store/` link; a dog tap wrote exactly one row (`{"code":"MILO","pet":"dog","lang":"ms"}`) to the stub. **Harness limitation worth knowing: `preview_click` reports success but delivers no events in this environment** (proved with capture-phase listeners on `document` — `window.__log` stayed empty and `location.hash` never changed), so every click in this verification was driven by dispatching a real `MouseEvent("click", {bubbles:true})` / `new Event("change")` on the same element via `preview_eval`. Identity sweep clean. **Named landing pages: one page per promotion, picked per label** (third same-day revision, `LETTER="b"` → **`"c"`** → plain **v123c**; still no engine bump) — her follow-up, verbatim: *"qr code should allow us to select their landing page / A landing page dropdown should allow us to select the page correspond to the intended function"*. I found the app already had a dropdown (`kindSel`, "What this code is for", four `KINDS`) and asked whether she meant renaming that, a library of named pages, or shared live pages; her answer — **"there will be more pages serving different promotion activity in future"** — settled it as the **library**, so the two earlier revisions' per-label words become the *top* layer of three. **The three layers, each line resolved on its own, per language:** the shared page (`settings.taster`), then the page the label picked, then the label's own line — so a page supplies the lines it has, everything it leaves blank still comes from the shared page, and a label on it can still shout a line over both. **`state.pages[]`** = `{id, name, heading, headingZh, headingMs, body, bodyZh, bodyMs, trOverride, trSrc, createdAt}` (the same six copy keys and the same provenance a label carries, which is what makes `isOverridden` / `markManual` and the whole `copyLine` / `fillAll` / `regenOne` machinery work on a page with **no new translation code**), and **`code.pageId`** (`""` = the shared page). **Resolution is at publish time, not render time** — `publishCodes` calls `pageLines(pageOf(state, c), out)` *before* `pageLines(c, out)` (the label still wins), so the published shape is **unchanged** and **neither `/taster/`, `mergeTaster`, `store/app.js`'s whitelist, the shop's code banner nor the printed label sheet needed an edit**; editing a page republishes every label on it through the page editor's own `maybeSyncStorefront`. `pageOf(state, code)` returns **null** for a blank *or deleted* id, so a deleted page's labels fall back rather than breaking (which is why `codeDraft` blanks a `pageId` that no longer resolves). **`pageStats(state, pageId)`** counts the labels on a page — shown on its row and in the delete confirm, because deleting a page moves every one of those labels back to the shared page's words. **`linesFor(state, code, shared)`** returns `shared` with the page laid over it and **deliberately NOT the label's own line**: it answers "what does the page say", which is exactly what an **empty box falls back to**, so the greyed hint is the line a customer would really get and a page's blank line shows the shared page's line rather than an empty box. That is why the code pop-up's `shared` became the **merged base** (`linesFor(state, draft, t)`) rather than just the shared page — pass the shared page alone and a page's blank line would grey out nothing where a customer really reads the shared one. **New view code:** exported pure **`pageDraft(page)`** (array-safe `trOverride` for the same reason `codeDraft` is — see the second revision), **`openPageForm(state, page, redraw)`** (a name box + the four `copyLine`s over the same generic `copyTarget`, `persist` a no-op so Save is the only writer, and `redraw` writing the name box back **before** `refresh()` because the body is rebuilt from `draft`), **`pagesCard(state, redraw)`** (a "Your pages" card with `＋ New page`, one row per page showing name + label count + Edit), the **"Landing page"** dropdown first in the code form (`— the shared page —` + every page, `keepTyped(); refresh()` on change), and the code row's sub-line naming the page when one is picked. `state.js` gained `findPage(state, id)`. **Two deliberate exclusions:** the **offer stays on the label** (a page carries words only — putting the offer on a page too gives one number two homes and a second inheritance rule to learn; *flagged to her as a small follow-up if she'd rather set an activity's offer once on its page*), and **no `active` flag on a page** (retiring its codes is how an activity is taken down; a flag would be a second way to do one thing). **New tests:** `test/pages.test.js` (14 — `pageOf` resolution incl. a deleted page, `pageStats` counting, the three layers, the blank-falls-through rule, **that `linesFor` stops at the page and does not fold in the label's own line** (the boundary that keeps the editor honest, with the same label proved against `publishCodes` in the same test), a blank page line staying *absent* rather than `""` in the published row, a deleted page's labels falling back while keeping their own lines, `pageDraft`'s array-safe copy + nothing reaching the record before Save + a wrong-shaped list dropped, `codeDraft` blanking a dangling `pageId`, and `normalize()` keeping `pages`) plus a `pages` row in `test/codes.test.js`'s defaults/normalize/older-save round-trips and a "a page written on one phone arrives on the other" case in `test/sync.test.js`. **`sync.js` `LISTS.pages`** is load-bearing, not tidiness: a label points at a page by id, so a page missing on her other phone means the label silently reads the shared page there. **1044 tests pass.** Docs: the Guide screen's Shops & codes card + a new "Your landing pages" card, `CHANGELOG.md` (a v123c section at the top, and the v123b line's "Every label opens the same page" amended), `README.md` (a "Landing pages: the middle layer" section — the publish-time snippet, the `linesFor` boundary, the `pageDraft` rule), `marketing/build_guide.py` (the chapter's "three cards" → **four**, a new "Your pages" bullet, a new "A page is live" bullet, the label's-own-words bullet reworded to three layers and "the page that label reads" instead of "the shared page", and `LETTER="c"`), and this file. **Sandbox-verified, both halves, and every PDF rebuilt (2026-09-20).** The three layers were proved end to end on the app's **own** published payload on port 8463 (fixture produced by `syncStorefront` itself through the new `.claude/serve-landing-stub.mjs`, so the shape checked is the shape she gets; nothing reached her Supabase): `SMPL1` (on page `pg1`, no own words) reads the page's line per language and **falls back to the shared page per language** — its 中文 is the *shared* `厨房直送` because `pg1` has no `headingZh`; `MILO2` (same page, own `heading`/`headingZh`) shows its own `New here?` / `我的标题` **and** the page's `Dari kaunter Paw Shop` in BM, the key proof that a label wins **per language, not wholesale**; `PLAIN` (no page) and `GONE4` (a **deleted** page) both read the shared page with no breakage; the offer and `from Paw Shop` localize in all three; `?c=` is handed on to the store. The admin screens were then checked at 375 px on port 8462 with seeded pages and labels: the **Your pages** card lists each page with its live label count, the **Landing page** dropdown reads `— the shared page — / Sample card / Raya promo 2026`, the code rows name their page, the code pop-up's greyed hints come from the **page** (`Dari kaunter Paw Shop`, not the shared page — `linesFor` doing its job), the `中文`/`BM` tags are present, the page editor opens with the name box + four `copyLine`s and no writer before Save, all 15 screens render with no `"null"`/`"undefined"` text and **no overflow**, and the Guide screen renders with the new card. `trOverride` was also proved live: with `["heading","headingZh"]` on a record, the 中文 box renders `is-mine` (hers) while BM stays greyed with the page's line — the array-safe `codeDraft`/`pageDraft` copy working in the browser, not just in a test. **Two traps worth remembering, both hit in this revision.** (1) **`node --check file.js` is a FALSE NEGATIVE on these files** — with no `"type":"module"` in `package.json` node parses them as CommonJS and bails at the first `import`/`export`, reporting success on a file that is genuinely broken; a newly added guide card was missing one `)` and node said "exit 0" while the browser refused the module and `/admin/#:codes` rendered **empty**. Always check ESM by copying to `.mjs` first (`cp f.js "$TMPDIR/f.mjs" && node --check "$TMPDIR/f.mjs"`), which is what finally located it at the file's closing parens rather than the offending card. (2) **fpdf2 renders `__italic__`, never `*italic*`** — four single-asterisk runs added to `build_guide.py` and `CHANGELOG.md` printed as literal `*Sample card*` in the PDFs; fixed to `__…__` and both PDFs re-read to prove zero literal runs remain. **The older 25 were fixed the same day (owner's go-ahead, 2026-09-20)** — they were a pre-existing defect across older entries, flagged first and rewritten only once she said so. The fix is in the **builders, not the sources**: `marketing/build_changelog.py`, `build_guide.py` and `build_money_guide.py` all gained an **`italics(s)`** helper (called first inside `clean()`), because `CHANGELOG.md` and the builders' prose stay ordinary markdown (`__x__` is *bold* in markdown, so rewriting the sources would have changed how they read on GitHub — and `build_money_guide.py` was found to have four of the same runs). It is a **left-to-right scan, not a search-and-replace**: runs sit back to back (`*Salary (you)*, *EPF / SOCSO*, *Marketing*`) and any pattern matching lone asterisks reads the closer of one as the opener of the next — a plain regex produced five bogus `*, *` pairs on that very line. `**bold**` runs are copied through untouched, and a block with an **odd** count of lone asterisks is handed on unchanged rather than guessed at (which is exactly what leaves the manual's three genuine literal asterisks alone: the required-field `*` in the details-form mock-up and the `select * from public.order_alert_errors` in the alerts section). Verified by extraction: `changelog.pdf` **0 asterisks / 78 italic spans**, money guide **0 / 39**, manual **3 / 80** (all three intended), market survey and postage explainer 0, and every converted run confirmed to carry **`Helvetica-Oblique`**. The manual is **34 pp at v123c** (`v123c` on all 34 pages, no stale `v123b`), `changelog.pdf` is 27 pp, and only the tracked root `changelog.pdf` sits at the repo root.

**Not done yet (owner go-live, she acts — I guide):**
1. **Supabase project: DONE** (2026-09-07) — her own project `ircwozniiyywsowamixy`, all core `.sql` run, creds wired into `store/config.js` + `admin/js/state.js`. The v61 `supabase/reviews.sql` + `supabase/backups.sql` are **DONE too** (owner ran them earlier; verified 2026-09-09 — the `reviews`, `backup_snapshots` tables and `review-photos` bucket all exist). The v62→v96, v99→v102, v103→v113, v114→v115, v116→v118 and v119→v123 syncs are all **code-only (no SQL)** (v97/v98 is the only SQL since v61). **NOT YET RUN: `supabase/track_no.sql`** (the v97/v98 tracking-number column) — she runs it once in her Supabase SQL editor; until then the tracking number reaches the WhatsApp message and the app but not the customer's page. **NOT YET RUN: `supabase/taster_visits.sql`** (the 2026-09-20 sales-codes/QR-label work) — also run once in the same SQL editor; until then a printed label still prints and still scans normally and **only the counts are missing**, and the **Label visits** card on More → 🏪 Shops & codes names the missing SQL rather than going blank. Nothing about the offer, the order stamp or the partner counting depends on it — the one thing it buys is knowing how many people opened each label and how many said dog or cat. Also **DONE 2026-09-09:** the optional `supabase/functions/wish-mail` Edge Function is **deployed + working** (Resend sends the wish list automatically; see memory [[wish-mail-resend-setup]] for the CORS gotcha).
2. **Domain: DONE in the repo** (CNAME = munchies.com.my). Outside the repo: point the domain's DNS at the site and enable GitHub Pages on the repo so munchies.com.my actually serves this folder (owner commits/pushes via GitHub Desktop first).
3. Send product list + photos → I fill the homepage grid (she edits the live menu via the admin app).
4. Set WhatsApp/Instagram, TNG QR, postage fee, PIN — Settings → Storefront on the phone.
5. Test on her phone: order → inbox → confirm → track; then I re-version + re-send the guide PDF.
6. **Order alerts (ntfy): script RUN by her 2026-09-18 — phones next.** `supabase/order_alerts.sql` — she ran it once in the SQL editor. Remaining: copy the topic it printed from the result grid (step 3 of the manual's section 15) into the free ntfy app on each phone, then place a test order. **Only a customer's order placed on the shop pings** — an order she types into the admin app does not, so a test made that way proves nothing. Nothing goes in the repo: **the topic IS the password**, generated by the SQL and stored in the `order_alert_topic` table — never print it in chat, never commit it. The design is the bakery's proven **insert-trigger** (`order_alert_trg` AFTER INSERT on `incoming_orders` where status='new' → security-definer `alert_new_order()` → dedupe into `order_alert_log` → `net.http_post` via pg_net to ntfy.sh; failures land in `order_alert_errors`). **Do NOT reintroduce pg_cron** — the bakery's one-minute timer ran fine and never delivered a ping. No engine bump (Supabase-only; the phones did not change).

**Open questions for her (not blockers, settle at go-live):**
- Is **Collect (local)** actually offered, or is delivery strictly postal? The copy currently keeps a "Collect (local)" option (homepage card + storefront toggle) — trim it if she is post-only.
- The occasion import catalogue (Delivery calendar → Add occasion) is byte-identical to the bakery, so its groups include "Baking & sweet days" (Cookie Day etc.) alongside pet/people days. If she doesn't want baking-promo days on a pet-treat calendar, the catalogue becomes a permanent jerky-localized file (trimmed) on the next sync — reversible either way.
- Product photos are placeholders ("PHOTO COMING SOON") until she sends images.
